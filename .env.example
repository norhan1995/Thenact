# Provision Neon from Vercel Marketplace; it supplies DATABASE_URL automatically.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Optional for local development only. Vercel production can authenticate
# to AI Gateway through the deployment's OIDC identity.
AI_GATEWAY_API_KEY=
