# Neon Postgres is injected automatically when connected through Vercel Marketplace.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Agent Mode. Keep this only in local/Vercel environment variables; never commit a real key.
OPENROUTER_API_KEY=
